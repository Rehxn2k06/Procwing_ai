"""backend/app/routers/whatsapp.py — Placeholder for the WhatsApp agent.

The WhatsApp agent owns this file.  This placeholder defines an empty router
so main.py can import and register it before that agent's implementation lands.
POST /api/whatsapp/webhook is documented in API_SPEC.md §9.
"""

from __future__ import annotations

from fastapi import APIRouter

router = APIRouter()
